{
  "name": "courtscan",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Every verdict on GenLayer, and whether it actually stands",
  "scripts": {
    "index": "node scripts/courtscan_index.mjs",
    "probe": "node scripts/assay_sweep.mjs",
    "dev": "cd app && npm install && npm run dev",
    "build": "cd app && npm install && npm run build",
    "test": "node --test tests/decode.test.mjs",
    "verify": "node scripts/verify_reports.mjs",
    "fixtures": "node scripts/grab_fixtures.mjs"
  },
  "dependencies": {
    "ethers": "^6.13.0",
    "genlayer-js": "1.1.8"
  }
}
